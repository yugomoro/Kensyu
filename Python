import math

for i in range(0, 360, 15):
    rad = math.radians(i)
    sin_val = math.sin(rad)
    cos_val = math.cos(rad)
    tan_val = math.tan(rad)
    print(f"sin({i}°) = {sin_val:.4f}, cos({i}°) = {cos_val:.4f}, tan({i}°) = {tan_val:.4f}")
