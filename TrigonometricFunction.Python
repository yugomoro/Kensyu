import math

for i in range(0, 361,15):
    rad = math.radians(i)
    sin_val = math.sin(rad)
    cos_val = math.cos(rad)
    tan_val = math.tan(rad)
    print(f"sin({i}°) = {sin_val}, cos({i}°) = {cos_val}, tan({i}°) = {tan_val}")
